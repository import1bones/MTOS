[package]
name = "shell"
version = "0.1.0"
edition = "2021"
authors = ["MTOS Project"]
description = "Simple shell userspace application for MTOS"

[dependencies]
mtos-runtime = { path = "../runtime" }
heapless = "0.7"

[[bin]]
name = "shell"
path = "src/main.rs"
